import React from "react";

function Todo(props) {
  return (
    <li id={props.id}>
      <p>{props.task}</p>
      {!props.isCompleted && <button onClick={props.handleComplete}>Complete</button>}
    </li>
  );
}

export default Todo;
